<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:memoryList"
             x:Class="memoryList.App">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
				<local:LightTheme/>
            </ResourceDictionary.MergedDictionaries>

            <!--Global Styles-->
            <Style TargetType="NavigationPage">
                <Setter Property="BarBackgroundColor" Value="{DynamicResource NavigationPrimary}" />
                <Setter Property="BarTextColor" Value="{DynamicResource NavigationTextColor}" />
            </Style>

            <Style
                TargetType="TabbedPage"
                ApplyToDerivedTypes="True"
            >
                <Setter Property="BarBackgroundColor"
                        Value="{DynamicResource NavigationPrimary}"/>
                <Setter Property="BarTextColor"
                        Value="{DynamicResource NavigationTextColor}"/>
                <Setter Property="BackgroundColor"
                        Value="{DynamicResource NavigationPrimary}"/>

            </Style>

        </ResourceDictionary>
    </Application.Resources>
    
</Application>